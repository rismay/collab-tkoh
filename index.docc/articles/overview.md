# Overview

@Metadata {
  @PageKind(article)
  @PageColor(indigo)
}

## Purpose

A simple, provisioned collaboration page.

## Working agreements (draft)

- **One thread = one intent** (spin when intent changes).
- Use durable markers: `DECISION:` / `TODO:` / `BLOCKED:`.
- Default to reversible steps.

## Next

- Add roles, scope, and first 1–3 workstreams once tkoh onboarding details are confirmed.
